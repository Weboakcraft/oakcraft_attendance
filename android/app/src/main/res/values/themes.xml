<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The launcher shows the splash while Chrome warms up, then hands over. -->
    <style name="Theme.Oakcraft.Launcher" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/plate</item>
        <item name="android:statusBarColor">@color/plate</item>
        <item name="android:navigationBarColor">@color/plate</item>
    </style>
</resources>
